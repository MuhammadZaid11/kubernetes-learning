curl -Lo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
chmod +x minikube
sudo mv minikube /usr/local/bin/

minikube start --driver=docker --vm=true 

minikube status

kubectl get nodes

kubectl get nodes --context kind-zaid-kind-cluster

kubectl config use-context kind-zaid-kind-cluster

minikube stop

minikube delete
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx_deployment
  namespace: nginx
spec:
  replica: 2
  selector:
    matchlabels:
      app: nginx
    
  template:
    metadata:
      labels:
        app: nginx



